name: build-environment
channels:
    - defaults
    - phygbu
    - conda-forge
dependencies:
   - conda-build
   - anaconda-client
   - setuptools >=61.0
   - wheel
   - build
   - twine
