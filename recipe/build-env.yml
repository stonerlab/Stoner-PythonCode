name: build-environment
channels:
    - defaults
    - phygbu
    - conda-forge
dependencies:
   - python >=3.12
   - conda-build
   - anaconda-client
   - setuptools >=61.0
   - wheel
   - build
   - twine
